# Packages

| Path | Role |
| --- | --- |
| `contracts` | `ShieldedPool`, verifiers, Foundry tests |
| `circuits` | Circom sources and proving-key layout |
| `sdk-core` | Notes, backup, Merkle, honesty helpers |
| `cli` | Reference CLI (`ap`) |
| `python-client` | Same protocol path via the Node CLI |
| `relayer` | Optional local Silent-send relayer |

Sepolia is live. Mainnet clients stay blocked. See the root [README](../README.md).
