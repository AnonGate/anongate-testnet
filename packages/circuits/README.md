# Circuits

Circom + Groth16 (Poseidon). Product circuits are depth 20: deposit, withdraw (2-in), withdraw 1-in, withdraw partial.

Live Sepolia verifiers were exported from Phase-2 ceremony finals. `*_dev` (depth 4) and `*_trusted` (single-party) are for tests only — not mainnet.

`.zkey` and `.wasm` files are not stored in git. See `keys/README.md` and `ceremony/finals/README.md`.

```bash
npm install
```
