# Ceremony artifacts

Live Sepolia proving keys and wasm are in [`finals/`](finals/). The full Phase-2 transcript (5 contributors + beacon) is published separately:

https://github.com/AnonGate/anongate-ceremony

Those finals are the same files intended for a later mainnet deploy. A ceremony is not an audit.

Local helper scripts (`ceremony_preflight.mjs`, contributor-kit) stay in this package. Local `*_trusted` hashes are not ceremony evidence.
