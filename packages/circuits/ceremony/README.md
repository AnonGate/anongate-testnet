# Ceremony artifacts

Place Phase-2 finals here after a real multi-party setup. `.zkey` files stay out of git.

- `ceremony_preflight.mjs` / `ap ceremony status` — freeze source and r1cs hashes
- `hash_ceremony_artifacts.mjs` — fingerprint local builds
- `contributor-kit/` — contributor machine instructions
- Copy `manifest.expected.template.json` to `manifest.expected.json` only after finals exist

Local `*_trusted` hashes are not ceremony evidence. Mainnet stays blocked.
