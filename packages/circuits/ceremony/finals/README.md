# Ceremony finals (published)

Phase-2 Groth16 proving keys and wasm used by the live Sepolia pools. These files are **public** — anyone generating a proof needs them. They are not spending secrets.

```
deposit_final.zkey + deposit.wasm + deposit_vkey.json
withdraw_final.zkey + withdraw.wasm + withdraw_vkey.json
withdraw_1in_final.zkey + withdraw_1in.wasm + withdraw_1in_vkey.json
withdraw_partial_final.zkey + withdraw_partial.wasm + withdraw_partial_vkey.json
```

Still **not** mainnet-safe: no external audit.

Full contribution chain: [anongate-ceremony](https://github.com/AnonGate/anongate-ceremony).
