# Ceremony finals

Place Phase-2 Groth16 outputs here:

```
deposit_final.zkey / deposit_vkey.json
withdraw_final.zkey / withdraw_vkey.json
withdraw_1in_final.zkey / withdraw_1in_vkey.json
withdraw_partial_final.zkey / withdraw_partial_vkey.json
```

`.zkey` files are gitignored. Current Sepolia pools use ceremony-exported verifiers. These keys are still **not** mainnet-safe (no external audit).
