# Obsolete deploy scripts

**Do not use.** These PowerShell helpers target depth-4 / redesign-era Sepolia deploys that are superseded by:

- `packages/contracts/script/DeploySepolia.s.sol` (depth-20 LOCAL TRUSTED)
- `packages/contracts/scripts/deploy-sepolia-depth20.mjs` (one-shot cast/forge path used for the live redeploy)

Kept for git history / archaeology only. Active operators must not run anything in this folder.
